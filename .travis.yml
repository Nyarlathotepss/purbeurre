language: python
python:
  - '3.6'

before_script:
  - pip install -r requirements.txt
  - psql -c "CREATE DATABASE purbeurre;" -U postgres

services:
  - postgresql

script:
  - sudo manage.py initdb
  - sudo python manage.py test